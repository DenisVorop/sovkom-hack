# sovkom-hack

## frontend

cd frontend 

`npm i` \
`npm start`

## backend

cd backend

`docker-compose -f db.yml up -d` \
`docker-compose -f frontend.yml up -d` \
`docker-compose -f backend.yml up -d`
